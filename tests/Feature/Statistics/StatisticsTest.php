<?php

namespace Tests\Feature;

use Tests\TestCase;

class StatisticsTest extends TestCase
{

    /**
     * No authentication -> assert authentication failed
     *
    public function testRoutesExist()
    {
        $this->get('/api/history')->assertStatus(200);
        $this->get('/api/hours')->assertStatus(200);
    }
*/
    /**
     * A basic functional test example.
     *
     * @return void
     *
    public function testHistoryRoute()
    {
    $response = $this->get('/api/history');

    $response->assertJsonStructure([

     ]);
    }
     * */

}
